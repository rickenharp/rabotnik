<%= form_for([@project, @keyword_import], url: project_keyword_import_path) do |f| %>
  <div class="field">
    <%= f.label :keywords %><br />
    <%= f.text_area :keywords, :cols => 40, :rows => 40%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
